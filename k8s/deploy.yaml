apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: dev-server
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: dev-server
    spec:
      containers:
      - name: dev-server
        image: 675208127568.dkr.ecr.us-east-2.amazonaws.com/devopsrepo:latest
        ports:
        - name: http-port
          containerPort: 8080
          protocol: TCP
